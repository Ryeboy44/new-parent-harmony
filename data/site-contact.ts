/** Primary public contact email — use for all mailto links and user-facing copy. */
export const SITE_EMAIL = "gemma@newparentharmony.com" as const;

export const siteMailtoHref = `mailto:${SITE_EMAIL}` as const;
