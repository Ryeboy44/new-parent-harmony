/** Primary conversion CTA — sitewide label and route (form on `/contact` via `/discovery-call`). */
export const PRIMARY_CTA_HREF = "/discovery-call" as const;
export const PRIMARY_CTA_LABEL = "Book a Free 15-Min Chat" as const;
