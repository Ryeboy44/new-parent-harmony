/** Image URLs from the current newparentharmony.com site (same assets as the live homepage). */
const NPH_ORIGIN = "https://www.newparentharmony.com";

export const nphSiteImages = {
  /** Homepage key visual (760×362 on the legacy site). */
  heroKeyVisual: `${NPH_ORIGIN}/image/141901001.png`,
  /** Founder portrait (full file; higher resolution than the scaled inline thumbnail). */
  gemmaPortrait: `${NPH_ORIGIN}/files/138733821.jpg`,
} as const;
